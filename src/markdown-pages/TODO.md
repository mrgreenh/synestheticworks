# TODO: delete this directory

These markdown files are leftover from the pre-Next.js Gatsby site (see
the "Migration to next js" commit, `150a638`). The live blog content
now lives in `/content/` and is read by `/lib/markdown.js`; nothing in
the current build references `src/markdown-pages/`.

These copies are stale (they don't carry the link fixes that landed on
their `/content/` siblings) and should be deleted in a future cleanup.
